import { ActionTree, GetterTree, MutationTree } from 'vuex'

const state: any = {
}

const getters = {
} as GetterTree<any, never>

const mutations = {
} as MutationTree<any>

const actions = {
} as ActionTree<any, never>

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
